/**
 * @typedef { import('@kano/kit-app-shell-core/types').RunConfig } RunConfig
 * @typedef { import('@kano/kit-app-shell-core/types').BuildConfig } BuildConfig
 * @typedef { import('@kano/kit-app-shell-core/types').TestConfig } TestConfig
 */

/**
 * @typedef {Object} WebConfig
 * @property {Number} port Port the run server will listen to
 * @property {BuildConfig} build Build config for the web platform
 * @property {RunConfig} run Run config for the web platform
 * @property {TestConfig} test Test config for the web platform
 */

export {};
